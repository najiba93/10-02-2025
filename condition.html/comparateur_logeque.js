/*---------------------------------les operateurs logiques---------------------------- */
let prenom
let loginAge
let loginPrenom
let age
//l'operateur et " && "

// si je cumule 2conditions
// si prenom === loginPrenom et age === loginAge

if( (prenom === loginPrenom) && (age === loginAge)){
// si ces 2 conditions sont remlis alors ici le code s'execute
}

if ( a && b ){// ici il faut 2 conditions pour que le code s'execute sinon le code ne se lance pas 

}

//operateur OU " || "

if( a || b ){
// ici il faut au moins une de ces 2 condition pour que le code s'execute
}
// l'operateur " ! " signifie le contraire
let userIslogin=true
if(!userIslogin){
    //est different de true 
    // si le user n'est pas connecte, si userIsLogin est false
}
ce qui revient a ecrire
if(userIsLogin===false){
    // si le user n'est pas connecte, si userIlogin est false
}
